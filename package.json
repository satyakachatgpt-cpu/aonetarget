{
  "name": "aonetarget-master",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo '🚀 Starting Dev Environment... (Client will wait for Server boot)' && concurrently --raw \"npm run server -s\" \"node -e \\\"setTimeout(process.exit, 3000)\\\" && npm run client -s\"",
    "server": "cd server && npm run dev -s",
    "client": "cd client && npm run dev -s",
    "install-all": "npm install && cd client && npm install --legacy-peer-deps && cd ../server && npm install",
    "build": "cd client && npm run build -s"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.3.1"
  },
  "dependencies": {
    "cloudinary": "^1.41.3",
    "multer": "^2.1.1",
    "multer-storage-cloudinary": "^4.0.0"
  }
}
