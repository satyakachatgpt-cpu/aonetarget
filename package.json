{
  "name": "aonetarget-master",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo '🚀 Starting Dev Environment... (Client will wait for Server boot)' && concurrently --raw \"npm run server -s\" \"node -e \\\"setTimeout(process.exit, 8000)\\\" && npm run client -s\"",
    "server": "cd server && npm run dev -s",
    "client": "cd client && npm run dev -s",
    "install-all": "npm install && cd client && npm install --legacy-peer-deps && cd ../server && npm install",
    "build": "cd client && npm run build -s"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.3.1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "express-mongo-sanitize": "^2.2.0",
    "helmet": "^8.1.0"
  }
}
